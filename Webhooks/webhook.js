require('dotenv').config();
require('http').config();
require('MessageEmbed').config;
client.on('request', request => {
  const webhook = new DiscordWebhook
    run: async () => {
      requests()
      .responses()
      .req()
      .res()
      .post()
      .ping()
      .WebhookClient('857776872959377468', 'token')
      .setChannel('<#851469487567405097>')
      .setMessage(RichEmbed);
      console.log('webhook Triggered');
    };
  },
);